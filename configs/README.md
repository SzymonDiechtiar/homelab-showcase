## Configuration Examples

This directory contains selected services and their configuration files from the homelab environment.

The configurations are simplified and sanitized to demonstrate key concepts in infrastructure management, including networking, reverse proxying, TLS automation, and storage.

Only a subset of services is included to highlight practical implementations rather than provide a complete production setup.